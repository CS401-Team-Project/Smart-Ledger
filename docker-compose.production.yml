version: '3.7'
services:

    api:
        build:
            context: ./Back-End
        environment:
            DEBUG: "false"

    client:
        build:
            context: ./Front-End
            dockerfile: Dockerfile-prod
        ports:
            - "80:80"
